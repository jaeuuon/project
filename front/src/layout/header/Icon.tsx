import { Grid } from '@mui/material';

import Logo from 'Logo';

const Icon = () => {
    return (
        <Grid id="grid-header-icon" item xs="auto">
            <Logo />
        </Grid>
    );
};

export default Icon;
