import { Grid } from '@mui/material';

import Logo from 'Logo';

const Icon = () => {
    return (
        <Grid id="layout-header-grid-icon" item xs="auto">
            <Logo />
        </Grid>
    );
};

export default Icon;
